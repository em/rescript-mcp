---
"rescript-mcp": minor
---

Bind the remaining public experimental task storage and low-level stream surfaces, including pluggable `TaskStore` and `TaskMessageQueue` construction, missing in-memory store and queue methods, and live ReScript coverage for low-level server task streams.
